CREATE TABLE IF NOT EXISTS stat_table(
    conflict_key INTEGER NOT NULL UNIQUE,
    launch_count INTEGER NOT NULL
);

getLaunchCount: SELECT launch_count FROM stat_table;

iterateLaunchCount {
    INSERT OR IGNORE INTO stat_table VALUES (1, 0);
    UPDATE stat_table SET launch_count = launch_count + 1;
}